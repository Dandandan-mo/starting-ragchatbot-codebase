{
    "name": "ragchatbot-frontend",
    "version": "1.0.0",
    "description": "Frontend tooling for RAG Chatbot",
    "private": true,
    "scripts": {
        "format": "prettier --write \"frontend/**/*.{js,css,html}\"",
        "format:check": "prettier --check \"frontend/**/*.{js,css,html}\"",
        "quality": "npm run format:check"
    },
    "devDependencies": {
        "prettier": "^3.3.3"
    }
}
